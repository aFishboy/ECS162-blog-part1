<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">   
    
    <!-- font awesome for the trashcan icon --> 
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/main-styles.css">  
    
    <title>{{{appName}}}</title>

</head>
<body>
    <header>
        <a href="/" class="logo">
            {{!-- 
                Image sourced from https://pixabay.com/illustrations/fish-cartoon-fish-cartoon-art-7899095/
                with free use under the Pixabay Content License
            --}}
            <img src="images/logo.png" class="header-logo" title="Cartoon Fish" alt="Cartoon fish looking to the right">
            <h1>{{{appName}}}</h1></a>
        <nav>
            <ul>
                <!-- conditional rendering based on variables - Is the user logged in?-->
                {{#if loggedIn}}
                    <li><a href="/">Home</a></li>
                    <li><a href="/profile">Profile</a></li>
                    <li><a href="/logout">Logout</a></li>
                    <li>
                        <a href="/profile" class="user-info">
                            <img src="{{#if user.avatar_url}}
                                            {{user.avatar_url}}
                                        {{else}}
                                            /avatar/{{user.username}}
                                        {{/if}}" 
                                        alt="User Avatar" 
                                        class="header-avatar">
                            {{user.username}}
                        </a>
                    </li>
                {{else}}
                    <li><a href="/">Home</a></li>
                    <li><a href="/login">Login/Register</a></li>
                {{/if}}
            </ul>
        </nav>
    </header>

    {{{body}}}

    <footer>
        <p>© {{{copyrightYear}}} {{{appName}}}. All rights reserved.</p>
    </footer>
</body>
</html>
